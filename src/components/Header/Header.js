import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <div>
            <h1 className="heading-title">Meal DB</h1>
        </div>
    );
};

export default Header;